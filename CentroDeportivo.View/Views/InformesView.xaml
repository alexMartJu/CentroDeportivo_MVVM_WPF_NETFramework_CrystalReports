<UserControl x:Class="CentroDeportivo.View.Views.InformesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CentroDeportivo.View.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#2B2E34"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,6,0,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                                <Setter TargetName="border" Property="Background" Value="#999999"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tarjeta con ComboBox + botones -->
        <Border Grid.Row="0" Style="{StaticResource Card}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Informes del centro deportivo"
                       FontWeight="Bold"
                       FontSize="15"
                       Foreground="#FF8C42"
                       Margin="0,0,0,12"/>

                <!-- ComboBox para el informe 2 -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Actividad para 'Reservas por actividad':"
                           Style="{StaticResource FieldLabel}"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                    <ComboBox Width="250"
                          ItemsSource="{Binding Actividades}"
                          SelectedItem="{Binding ActividadSeleccionada}"
                          DisplayMemberPath="Nombre"/>
                </StackPanel>

                <!-- Botones -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="Informe de socios"
                        Width="180" Margin="10,0"
                        Background="#3498DB"
                        Style="{StaticResource ActionButton}"
                        Command="{Binding InformeSociosCommand}"/>

                    <Button Content="Reservas por actividad"
                        Width="200" Margin="10,0"
                        Background="#2ECC71"
                        Style="{StaticResource ActionButton}"
                        Command="{Binding InformeReservasActividadCommand}"/>

                    <Button Content="Historial de reservas"
                        Width="200" Margin="10,0"
                        Background="#E67E22"
                        Style="{StaticResource ActionButton}"
                        Command="{Binding InformeHistorialCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <TextBlock Text="Seleccione una actividad (si procede) y pulse el informe que desea generar."
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Foreground="#555"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   Margin="20"/>
        </Border>
    </Grid>
</UserControl>
